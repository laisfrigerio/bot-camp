import styled from 'styled-components';

const FooterWrapper = styled.footer`
    display: flex;
    position: fixed;
    bottom: 0;
    width: 100vw;
    height: 55px;
`;

export default FooterWrapper;
