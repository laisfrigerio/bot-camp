import styled from 'styled-components';

const ChatFooterWrapper = styled.footer`
    display: flex;
    position: fixed;
    bottom: 0;
    box-shadow: 0 -2px 8px 0 rgba(0,0,0,0.1);
    background-color: #fbfbff;
    width: 100vw;
    height: 55px;
`;

export default ChatFooterWrapper;
